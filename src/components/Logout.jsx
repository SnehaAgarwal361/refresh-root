import React from 'react';
import { FormattedMessage } from 'react-intl';

const Logout = () => {
  return (
    <div className="fluid">
      <FormattedMessage id="logout.head" />
    </div>
  );
}

export default Logout;
